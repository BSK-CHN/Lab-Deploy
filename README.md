# Lab-Deploy

This repository holds the Kubernetes manifests used by Argo CD.

## Local test

If you have a local Kubernetes cluster, apply the manifest with:

```bash
kubectl apply -f k8s/deployment.yaml
```
